#include<bits/stdc++.h>
#include <iostream>
using namespace std;
int main()
{
    int t;
    
    cin>>t;
    
    while(t--){
        
        vector<int>arr(3);
        
        for(int i = 0 ; i < 3 ; i++){
            cin>>arr[i];
        }
        
        sort(arr.rbegin() , arr.rend());
        
        if((arr[0] + arr[1]) >= 10){
            cout<<"yes"<<endl;
        }else{
            cout<<"no"<<endl;
        }
        
    }

    return 0;
}
